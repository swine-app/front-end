apiVersion: v1
kind: Service
metadata:
  name: swine-frontend
spec:
  type: LoadBalancer
  selector:
    app: swine-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
